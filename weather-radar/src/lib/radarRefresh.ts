/** Poll station radar frame lists (NEXRAD / OPERA) — new volume scans every ~2–6 min. */
export const STATION_RADAR_REFRESH_MS = 2 * 60 * 1000;

/** Poll RainViewer composite mosaic — updates about every 10 min. */
export const RAINVIEWER_REFRESH_MS = 5 * 60 * 1000;
